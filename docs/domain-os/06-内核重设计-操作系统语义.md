# 06 · 内核重设计：操作系统语义

> 分支 `feat/domain-os-kernel-redesign`。本文档定义第二代内核（包 `dos/`）。
> 它**不沿用**第一代（`domain_os/`，见 00–05 篇）的 Resource / Observation /
> Projection / Intent → Policy → Command → acknowledged → Reconcile → confirmed
> 抽象——那套本质上是"审批工作流"。第二代回到操作系统本身解决了什么这个问题。

## 1. 判断标准

一个 OS 的味道不来自概念名词，而来自三条铁律：

1. **内核不信任用户态**：智能体对世界的一切访问都经过统一 syscall 入口，逐条做能力检查与审计；
2. **世界状态只有一份可信副本**：在内核里，用户态只能拿到带代次的快照；
3. **发出命令不等于改变现实**：命令开启事务，由后续遥测证据经一致性检查器裁决。

Agent Runtime 之上缺的正是这五样东西（等价于没有 OS 的裸机编程）：统一设备访问层、唯一可信状态、syscall 边界、进程管理、一致性保障。**dos 的使命：把领域世界变成一台虚拟机，让任何智能体像调 syscall 一样安全地感知和操作它。**

## 2. 内核结构（每个模块是一个 OS 器官）

| 模块 | OS 角色 | 职责 |
|---|---|---|
| `dos/journal.py` | WAL | 只追加的不可变事实日志；一切生效前先记账 |
| `dos/namespace.py` | 文件系统 + 页缓存 + inotify | 世界挂载为层次路径空间；commit 推进 generation；`watch` 订阅；`derive` 注册可失效重建的派生视图（缓存失效有统一定义：依赖上游 generation 前进即作废） |
| `dos/devices.py` | 驱动 + 中断 | 驱动三角色：top-half 收中断（原始不可信）、bottom-half `normalize` 归一化提交、downlink `dispatch`；`privileged_actions` 声明需特权提升的动作；`verify` 是 fsck 规则 |
| `dos/capabilities.py` | 能力令牌 | 不可伪造的随机令牌是权威的唯一通货；grant 到子树 + 动作集合 |
| `dos/consistency.py` | fsck + 看门狗 | 未决事务：遥测证实 → committed；证伪 → failed；超时 → **unknown 并冻结该路径**（重开闸门不是幂等的，绝不盲目重发）；`thaw` 显式解冻 |
| `dos/process.py` | systemd unit + 调度器 | 智能业务过程 = 受监督用户进程：声明 watches/能力/优先级/重启策略；调度器按事件唤醒、按优先级执行；进程间只经命名空间与 syscall 通信，无共享内存 |
| `dos/kernel.py` | 内核装配 | mount / grant / spawn；syscall 面 `read` `watch` `derive` `act` `approve`；`pump()` 一个确定性内核节拍：泄中断 → journal → namespace commit → fsck → 调度一个周期 |

## 3. `act()` —— 唯一的变更 syscall

```
act(token, path, action, args, expect)
  1. 冻结检查       路径若有 unknown 未决事务 → FrozenPathError
  2. 能力检查       令牌须覆盖 path+action，否则 CapabilityError（且不记账）
  3. 前置校验       expect（路径→期望值）不成立 → PreconditionError（CAS 语义）
  4. 记账           journal 先写 intent，再有任何副作用
  5. 特权门         privileged 动作挂起为 awaiting_approval，approve()（pkexec）后继续，审批本身入 journal
  6. 派发           driver.dispatch 下发；开未决事务与期限；现实是否改变由遥测经 fsck 裁决
```

事务状态机：`open → (awaiting_approval) → dispatched → committed | failed | unknown`。

## 4. 闭环全景

```
设备(遥测站/业务系统/模型服务)
  │ 中断（原始、不可信）
  ▼
驱动 normalize → Journal 追加（不可变事实）→ Namespace commit（generation 前进）
  ▼
watch 唤醒 → 调度器按优先级运行进程（智能体在用户态 handler 内被调用）
  ▼
read/derive 命名空间 → 确定性模型判断
  ▼
act() → 能力→前置→记账→(审批)→派发
  ▼
新中断 → fsck 裁决未决事务 → committed / failed / unknown(冻结)
```

进程骨架由内核确定性管理：模型挂了按重启策略兜底，超限进 `failed`——"大模型组织专业能力，而不是替代专业能力"。

## 5. 领域无关性

水利只是第一个"安装实例"：`domains/flood/dos_instance.py` 把 808J1510 遥测站
mount 到 `/hydro/shanhu`，注册派生视图 `views/level_status`，spawn 水位监视
进程。换电网即 `/grid/...` + 新驱动；换设备只写驱动，换业务只写进程单元与
确定性函数，**内核代码零改动**。第一代 `domain_os/` 在迁移期间保留并继续
服务现有 server。

## 6. 与第一代的对应与刻意差异

第一代的正确工程直觉在此不是孤立概念，而是 OS 机制的自然推论：

- Projection 可重建 → 派生视图 = 页缓存（可随时丢弃重建，失效规则统一）
- Command 需确认、outcome_unknown 不重发 → 未决事务 + fsck + 路径冻结
- 审批流 → 特权提升（approve 本身入 journal，可审计）
- 策略约束 → 能力令牌（授予即范围，检查在 syscall 边界）

刻意不做的：不做完整插件框架，扩展点仍是五个——设备驱动、能力授予、派生
视图、进程单元、fsck 规则。

## 7. 常驻实体与观察者 agent

唤醒源与实体类型是正交的两个维度：确定性进程与模型驱动的 agent 都可以
被世界唤醒（观察）或被人/任务唤醒（对话）。dos 对四象限一视同仁——
syscall 面不区分调用者。**观察者 agent**（世界唤醒的 agent）不需要任何
专用机制，驻法：

```
agent runtime 内常驻循环：
  open_session(principal, read_scopes, act_...)      长命会话
  loop:
    wait_for_change(关心的路径, timeout=节拍秒数)       变化即醒；超时即周期醒
    history / read_path 观察 → 模型推理
    需要动手 → act（与进程的命令同一条管线、同一审批门）
    判断产物要留在世界 → act 于计算设备（file_assessment 等），
                         落盘工件 + 命名空间摘要，绝不直接写命名空间
```

三条纪律：观察者动手仍走 act+审批；观察产出入世界必须经计算设备事务
（版本、审计、generation 与模型预测平权）；**守夜者必须被监督**——外部
观察者的重启/心跳/死亡告警由其 agent runtime 负责（如 OAG harness），这是
部署观察者的准入条件，内核不感知外部会话死活。

进程侧对应补齐了第二个唤醒源：`ProcessSpec.every_seconds` 定时节拍（与
watches 变化唤醒并列）。agent 侧的节拍由 `wait_for_change` 的超时天然承
载（空路径列表 = 纯定时等待）。

已知边界：长命会话无空闲过期/心跳（观察者死亡后会话与能力令牌残留），
真实部署前补；MCP 推送通知（长轮询的延迟优化）暂缓。

## 8. 现状与下一步

已实现（本分支）：

- 内核七模块（`dos/`）；闭环演示 `scripts/check_dos_flood.py`（感知→判断→
  控制→反馈→审计回放，含幂等去重演示）；单测 `tests/test_dos_kernel.py`。
- **正确性**：`act()` 幂等（对在途同键事务去重复用）；fsck 证据时效（仅
  journal 序号晚于派发的遥测可提交事务）；mount 前缀冲突显式拒绝；watch
  回调异常隔离（不得污染 commit 路径）；驱动 `validate` 钩子在开事务前
  拒绝非法参数（垃圾不进 journal）。
- **并发**：内核大锁只覆盖单个 syscall 与 pump 的内核侧区段；运行用户进程
  前放下锁（handler 在工作线程执行、可再入 syscall），调度器防重入——如
  同真实内核运行用户态前释放锁。
- **监督**：进程时间预算（超时判失败、上下文隔离检疫，后续 syscall 拒
  绝）；审批挂起超时；重启策略与退避。
- **持久化**：Journal JSONL 落盘；`recover()` 开机 fsck——重建命名空间、
  能力令牌、未决事务与冻结路径；未决事务获得新的宽限期（重启不得立刻
  超时），冻结跨重启存活。
- **MQTT 驱动**（`dos/mqtt.py`，线格式沿用第一代验证过的约定）：同步传输
  层（内存总线供确定性测试 + Paho TLS/QoS1/重连重订阅）；`MqttTelemetryDriver`
  top-half 只排队中断（paho 网络线程安全）、bottom-half 校验并按
  `message_id` 去重（QoS 1 至少一次）、坏帧计数不致命；命令经
  `{prefix}/stations/{id}/commands` 下发；fsck 以新于派发的遥测为准。
  `scripts/check_dos_mqtt.py` 对 test.mosquitto.org 真实闭环冒烟通过
  （含 journal 落盘重放恢复）。洪水实例词汇与线上格式统一
  （`level_m` / `sampling_interval_seconds` / `set_sampling_interval`），
  模拟驱动与 MQTT 驱动可互换。
- **MCP 网关**（`dos/gateway.py` + `dos/mcp_server.py`）：syscall 面的线上
  形态，向任意持 MCP 客户端的 Agent Runtime（Python OAG、TS pi agent）
  开放。分层：gateway 是纯线程、可单测的会话/治理层（不依赖 mcp 库），
  mcp_server 把八个工具架上去（stdio 传输）。治理落点：会话（principal
  → session_id，能力令牌不出内核进程）；读域（read/list/watch 一律按
  会话声明的前缀过滤，`list_paths` 只回域内路径）；watch 桥接（长轮询
  `wait_for_change`，按 generation 前进唤醒）；审批工作清单
  （`pending_approvals` 拉取，push 通知留给值班 UI 集成）。
  `scripts/check_dos_mcp.py` 用真实 MCP 客户端跨进程走完整回路：会话 →
  域内读 → 越域拒绝 → 特权 act 挂起 → 审批 → 遥测证实事务提交 →
  watch 观测 generation 前进。引导姿态：open_session 信任传输层身份，
  生产部署应配认证传输（streamable HTTP + OAuth），`DOS_ACT_GRANT=0`
  可关掉网关铸造 act 能力。

- **观测镜像**（`dos/history.py`）：现实世界的数字镜像——每路径按**世界时间**
  （observed_at）索引的原始观测环，保留策略内热查询；journal（全量事实、
  落盘）→ mirror（近期可查询）→ namespace（当前值）三层各司其职。驱动契约
  支持 `(path, value, observed_at)` 三元组，遥测帧的 observed_at 直达
  journal 与镜像。**镜像只存原始观测、不做聚合**：窗口等应用级聚合是消费
  者的事（mpstat 模式——内核提供 /proc/stat，利用率由用户态算）；journal
  增设内存热尾窗。`history` syscall / 网关工具（受读域约束）/ 进程
  `ctx.history`。
- **洪水预测与影响评估迁移**（`domains/flood/dos_forecast.py`、`dos_impact.py`）：
  建立两个通用模式——
  1. **计算设备**：昂贵参数化计算（CNN 预测、影响评估）是设备不是视图。
     `act(run_forecast)` 开事务（输入快照全额入 journal，审计可回放）→
     工作线程执行 runner → 作业完成作为"遥测"自中断 → 元数据提交命名空间
     （大产物只在盘上，namespace 存句柄与摘要）→ fsck 按**新于派发**的作业
     证据提交/证伪；作业失败显式 `failed`（原因透传），超时 `unknown` 并
     冻结重跑——昂贵作业绝不盲目重试。forecast 身份来自 journal 序号
     （`fcst_{dispatched_seq}`），无额外纪元机制。per-action 事务期限
     （`Driver.deadline_for`）覆盖分钟级作业。
  2. **无状态触发进程**：24h 窗口聚合是进程私有决策依据——每次醒来从镜像
     重查窗口（25 点恰好对齐第一代窗口点数），无任何跨唤醒状态，崩溃重启
     即恢复；防重复触发 = 在途作业输入覆盖检查 + 已提交预测 `valid_from`
     覆盖检查。输入平面就是 MQTT 遥测站（四边界 = 四站 flow_m3s 指标），
     "演进 CSV"降级为测试馈送器概念，内核与领域装配里不存在。
  影响评估是计算设备模式的第二次实例化：无状态 sweep 进程 watch
  forecasts/latest 对标准目标集自动评估，agent 可经网关按需发起任意目标。
  `scripts/check_dos_forecast.py` 全链路演示（默认 fake 模型，`--real` 走
  真 CNN）。

已知边界（刻意）：内核能力模型只治理变更（act），读域治理在网关会话层；
open_session 的身份信任传输层；边界站未对齐时间戳时触发回退为"各站最新值
求和"（真实部署需重采样对齐，属应用层）；真实影响分析 runner 与避洪路线
尚未接入 dos（下一迭代）。

下一步候选：

1. 真实影响评估 runner 接入（复用 analyze_inundation_impacts 与网格索引）
   + 避洪路线（高德）作为第三个计算设备；
2. `scripts/dos_mcp_server.py` 换装预测/影响闭环（agent 经 MCP 触发研判的
   真实业务场景）；
3. server 前端 repoint（旧内核 → dos，回放经网关 history 工具）；
4. RemoteDriver 线协议（等第一个进程内够不着的设备）。
