[project]
name = "flood-emergency-agent"
version = "0.1.0"
description = "基于大模型的水路联动应急智能体集群应用"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "oag",
    "torch>=2.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.uv.sources]
oag = { path = "agent", editable = true }
